
// A function for setting an variable
myFunction(a: ref(bool), b: bool): void {
    a <- b
}

// The Main function, the first function that will
// be executed
main(): i32 {
    myBoolean = stack(true)
    myFunction(myBoolean, false)

    a = i32(10)
    c = a < 2

    0
}