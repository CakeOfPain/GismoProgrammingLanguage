puts(message: str)

add(a: ref(i32), b: ref(i32)): i32 {
    a <- i32(30)
    deref(a) + deref(b)
}

main(): i32 {
    a = stack(i32(10)) // ref(i32)
    b = stack(i32(20)) // ref(i32)
    c = stack(add(a, b))
    c <- add(c, b)

    deref(c)
}