// Keywords
$KEYWORD(ref)
$KEYWORD(deref)
$KEYWORD(stack)
$KEYWORD($NEWADDR)


$NEWADDR() ::= $CAT("%t", $IOTA())

// Builtin Types
$LOAD("toolchain/language/types.gsm")

// Global Contants
symbol = int ::= {
    $EXPORT($1, $2)
}

symbol = string ::= {
    $EXPORT($1, $2)
}

symbol = float ::= {
    $EXPORT($1, $2)
}


$LOAD("toolchain/language/functiondef.gsm")