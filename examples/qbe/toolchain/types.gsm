
$Type ::= $struct(
    id,
    prefix,
    nativeType,
    nativeMemoryType,
    typeName,
    constantType,
    metaData,
    memory_size
)

$Function ::= $struct(
    name,
    returnType,
    arguments,
    addr)

$Variable ::= $struct(
    name,
    type,
    addr)

$Value ::= $struct(
    type,
    addr)