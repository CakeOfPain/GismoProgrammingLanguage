
$Type ::= $struct(
    id,
    prefix,
    nativeType,
    nativeMemoryType,
    typeName,
    constantType,
    metaData
)

$Function ::= $struct(
    name,
    returnType,
    arguments,
    addr)

$Variable ::= $struct(
    name,
    type,
    addr)

$Value ::= $struct(
    type,
    addr)