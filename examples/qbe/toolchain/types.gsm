
$Type ::= $struct(
    id,
    prefix,
    nativeType,
    typeName,
    constantType
)

$Function ::= $struct(
    name,
    returnType,
    arguments,
    addr)

$Variable ::= $struct(
    name,
    type,
    addr)

$Value ::= $struct(
    type,
    addr)